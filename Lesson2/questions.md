1. Semantik (anlamsal) ve semantik olmayan HTML elemementleri (etiketleri) arasındaki temel fark nedir?
(What is the core difference between semantic and non-semantic HTML elements?)

2. 5 tane semantik HTML elemementi (etiketi) sayabilir misiniz?
(Can you name 5 semantic elements?)

3. Bir HTML sayfasına CSS eklemenin veya bağlamanın üç yolu nedir ve modern geliştirmede bunlardan hangisi en iyi pratik (best practice) olarak kabul edilir?
(What are the three ways to link or add CSS to an HTML page, and which one is considered best practice for modern development?)

4. CSS yazarken, elementleri stillendirmek için neden genellikle sınıf (class) seçicileri, kimlik (ID) veya etiket (tag) seçicilerine tercih edilir?
(When writing CSS, why are class selectors generally preferred over ID selectors or tag selectors for styling elements?)

5. Bir form oluşturmak ve farklı kullanıcı girdilerini toplamak için kullanılan başlıca HTML elemementleri (etiketleri) nelerdir?
(What are the primary HTML tags used to build a form and collect different types of user input?)

6. Arka planın (backend) gönderilen veriyi tanımlayabilmesi ve işleyebilmesi için bir <input> veya <textarea> etiketine hangi özel öznitelik (attribute) eklenmelidir?
(Which specific attribute must be added to an <input> or <textarea> so that the backend can identify and process the submitted data?)