1. What does `display: flex;` do to the children of an element?
   Türkçe: `display: flex;` bir elementin içindeki çocuk elementlere ne yapar?

2. What is the difference between `justify-content` and `align-items`?
   Türkçe: `justify-content` ve `align-items` arasındaki fark nedir?

3. What happens when we use `flex-direction: column` instead of `row`?
   Türkçe: `flex-direction:row` yerine `flex-direction: column` kullandığımızda ne olur?

4. Can `display: block;` and `display: flex;` be applied to the same element at the same time?
   Türkçe: Aynı elemente aynı anda hem `display: block;` hem de `display: flex;` uygulanabilir mi?
